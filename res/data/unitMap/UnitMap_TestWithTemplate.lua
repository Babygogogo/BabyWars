
local UnitMap_TemplateTest = {
    template = "UnitMap_Template",

    grids = {
        -- There's a template map, so that the grids data is ignored even if it's not empty.
    },
}

return UnitMap_TemplateTest
